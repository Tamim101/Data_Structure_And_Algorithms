#include<stdio.h>
#include<string.h>
char* longestPalindrome(char* s) {
    int start = 0;
    int end = 0;
    for(int i = 0; i < s; i++){
        for(int j = i+1; j < s; j++){
            
        }
    }
}
int main(){
    char array[] = {"abba"};
    int length = sizeof(array) / sizeof(array[0]);

}